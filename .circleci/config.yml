version: 2.0

orbs:
  maven: circleci/maven@1.0.1
jobs:
  build:
    machine: true
    steps:
      - checkout

      - maven/with_cache:
          steps:
            - run: mvn -q test verify -Pcoverage
      - maven/process_test_results
workflows:
  build:
    jobs:
      - build
